import React, {Component} from 'react';
import Menu from '../Menu';

class Nav extends Component {
    constructor(...props){
        super(...props);
    }

    render() {
        return (
            <Menu/>
        );
    }
}